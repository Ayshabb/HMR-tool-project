
---

### **Next steps**
1. Save this as `README.md` in your project root.  
2. In your terminal (inside `hmr-tool`), run:

```bash
git add README.md
git commit -m "Add README with project details and live link"
git push
